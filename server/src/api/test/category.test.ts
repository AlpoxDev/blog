import request from 'supertest';
import app from '../../index';

beforeAll(async () => {});

describe('Category - Main, Sub API TEST', () => {
  // test('200 | GET /categorys', async () => {
  //   const response = await request(app).get('/categorys?nickname=test');
  //   expect(response.status).toEqual(200);
  // });
});
