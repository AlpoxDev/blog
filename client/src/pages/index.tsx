import React from 'react';

import { LandingContainer } from 'containers/index';

const Page = (): React.ReactElement => {
  return (
    <>
      <LandingContainer />
    </>
  );
};

export default Page;
