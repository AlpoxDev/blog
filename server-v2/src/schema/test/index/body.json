{
  "title": "Test Index Body",
  "type": "object",
  "properties": {
    "accessToken": {
      "description": "소셜 토큰",
      "type": "string"
    },
    "socialType": {
      "description": "소셜 타입",
      "type": "string",
      "enum": ["KAKAO", "NAVER"]
    }
  },
  "required": ["accessToken"]
}
